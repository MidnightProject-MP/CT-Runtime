# Outer loop proof

This is an experimental vNext path. It proves Feedback wake → Work Unit reconstruction → justified bounded Execution → #22 turn → evidence/continuation persistence → end. The old architecture is not a compatibility target.