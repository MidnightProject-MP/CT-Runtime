# CT-Runtime V2 — capability-first

V2 separates disposable execution from authoritative state **behind capabilities**. Celestan requests `durable_state`, `evidence_store`, `knowledge_publishing`, `project_system`, `code_repository`, `scheduler` — not Neon, R2, Jira, GitHub, etc. Bindings select the adapter; Celestan logic does not change when Neon→Supabase or R2→S3 or Jira→Linear.

- `durable_state` → `postgres` adapter (standard `pg`, works with Neon/Supabase/self-hosted) or `filesystem` (local/test). Owns deployments, work orders, executions, attempts, topology, leases, schedules, events, telemetry, evidence indexes, and Observer lifecycle transactionally.
- `evidence_store` → `s3` adapter (standard S3, works with R2/S3/MinIO) or `filesystem`. Owns bounded raw evidence; Postgres holds only URI/hash/size.
- `knowledge_publishing` → `git` (Markdown) or `confluence`/`obsidian`. Celestan: `publishChronicle({period, markdown})`.
- `project_system` → `jira` / `linear` / `github_issues` per-project (e.g., `BorderCrossing→jira`, `CT-Foundry→github_issues`).
- `code_repository` → `github` / `gitlab`.
- `scheduler` → `cloud_scheduler` / `systemd_timer` / `cron` — mechanical only.

Observer hashes canonical immutable digest content while excluding only generated `observedAt`. Digest insertion, semantic-task indexing, lifecycle transitions, semantic sidecars, and execution Observer timestamps are reconciled transactionally. Authoritative model envelopes and host samples use separate tables and APIs. Export always uses Foundry's validated joined projection.

`FilesystemStore` / filesystem evidence remain the local compatibility adapters. `CT_RUNTIME_MODE=production` resolves bindings to `durable_state:postgres` + `evidence_store:s3` and fails closed; it never falls back to local files. `migrate --database-url URL` is an explicit migration-role command. Normal jobs do not migrate. See `capabilities/README.md`, `bindings.example.json`, and `lib/capabilities/` for contracts and `request('durable_state', {project})` usage.

## Retention

Use `ephemeral`, `operational`, `audit`, `observer-ledger`, `chronicle`, and `quarantine` classes. Raw operational evidence may expire only after its digest, hash, reference, and required provenance are durable. Digest, semantic, lifecycle, policy, consolidation, and Chronicle records are append-only; corrections are new records.

## Deployment

Cloud Run Jobs and Oracle A1 use the same image digest and CLI. Cloud scheduling is external. Oracle systemd templates and the concrete Cloud Run deployment script are in `deploy/`. No local disk is authoritative. Using PostgreSQL, S3-compatible APIs, OCI images, and two example compute providers improves replaceability but is not a claim of zero vendor lock-in.

## Verification status

The existing filesystem suites have local evidence only when rerun against the current tree. Container, PostgreSQL, MinIO, Cloud Run, Oracle A1, real S3, multi-architecture publication, and forced-termination continuity remain unverified until their CI/tooling/credential gates run. No deployment or multi-architecture success is claimed here.

Free allocations are account, billing, region, and policy dependent and can change. Google's official pricing currently lists a Cloud Run monthly free tier measured in vCPU-seconds and GiB-seconds, and Cloud Scheduler pricing lists three jobs per billing account at no charge. Oracle documents Always Free Ampere A1 compute as up to 3,000 OCPU-hours and 18,000 GB-hours per month in the home region. Verify current eligibility before deployment; these facts are not a guarantee of capacity or a no-cost bill.

Official references: [Cloud Run pricing](https://cloud.google.com/run/pricing), [Cloud Run Jobs](https://cloud.google.com/run/docs/create-jobs), [Cloud Scheduler pricing](https://cloud.google.com/scheduler/pricing), [Cloud Scheduler](https://cloud.google.com/scheduler/docs), [Oracle Always Free resources](https://docs.oracle.com/en-us/iaas/Content/FreeTier/freetier_topic-Always_Free_Resources.htm), [Oracle Ampere A1](https://www.oracle.com/cloud/compute/arm/), [PostgreSQL locking](https://www.postgresql.org/docs/current/explicit-locking.html), [S3 API](https://docs.aws.amazon.com/AmazonS3/latest/API/Welcome.html).
